Read docs/CLAUDE.md and docs/CONTRIBUTING.md for full project context, architecture, and conventions.

Key rules: ESM only (.js imports), no default exports, providers are pure (return data, don't mutate config), all URLs in src/providers/links.ts, secrets via stdin never CLI args, native fetch not axios, build before CLI tests.
